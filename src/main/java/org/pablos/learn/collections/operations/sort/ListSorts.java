package org.pablos.learn.collections.operations.sort;

import java.util.Arrays;

public class ListSorts {

    void sortArray(int[] values) {
        Arrays.sort(values);
    }

    void sortArray(String[] names) {
        Arrays.sort(names);
    }
}
